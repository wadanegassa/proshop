import 'package:flutter/material.dart';

class AdminColors {
  // Dark Theme Palette (Larkon Aesthetic)
  static const Color background = Color(0xFF141419); // Deep dark background
  static const Color sidebar = Color(0xFF1B1B21); // Slightly lighter sidebar
  static const Color surface = Color(0xFF202026); // Card surface
  static const Color card = Color(0xFF202026);
  static const Color divider = Color(0xFF2A2A32); // Subtle divider

  // Accents
  static const Color primary = Color(0xFFFF6B00); // More vibrant orange
  static const Color secondary = Color(0xFF6C757D);
  static const Color success = Color(0xFF28A745);
  static const Color error = Color(0xFFDC3545);
  static const Color info = Color(0xFF17A2B8);
  static const Color warning = Color(0xFFFFC107);

  // Text
  static const Color textPrimary = Color(0xFFE9ECEF);
  static const Color textSecondary = Color(0xFFADB5BD);
  static const Color textMuted = Color(0xFF6C757D);

  // Chart Colors
  static const Color chartPrimary = Color(0xFFFD7E14);
  static const Color chartSecondary = Color(0xFF3D3D47);
}
